[project]
name = "vllm-ft-build"
version = "0.1.1"
requires-python = ">=3.14"

dependencies = [
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
